# SQLAlchemy ORM Models Placeholder

This directory will contain database table ORM models for:
- `users`: User profiles and settings (future authentication)
- `orders`: Paper trading orders (BUY/SELL)
- `positions`: Active and historical paper trading positions
- `wallets`: Virtual cash balances and margin accounts
- `predictions`: Logged prediction outputs and regime states for auditing
- `backtests`: Stored backtest runs and performance logs
